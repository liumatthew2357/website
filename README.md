# matthewliu.ca

A static personal website built with semantic HTML, modern CSS, and vanilla JavaScript for deployment on GitHub Pages.

## Preview locally

Open `index.html` directly, or serve the directory with any static file server.

## Current status

The first implemented slice is the homepage and global responsive navigation. Interior routes will be built iteratively as their content is finalized.
